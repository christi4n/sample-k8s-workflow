format_version: 3
pipelines:
  check_environment_vars:
    group: demo
    label_template: ${COUNT}
    lock_behavior: none
    display_order: 4
    environment_variables:
    stages:
    - check_environment_vars:
      approval:
          type: success
      jobs:
        check:
          timeout: 0
          elastic_profile_id: demo-app
          tasks:
          - exec:
              arguments:
              - -c
              - printenv
              command: /bin/bash
              run_if: passed
